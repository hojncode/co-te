// function solution(array) {
//   var answer = 0;

const { arrayBuffer } = require("stream/consumers");

      
//   const obj =  array.reduce((acc, curr) => {
//       if (curr in acc) {
//           acc[curr]++;
//       } else {
//           acc[curr] = 1
//       }
//       return acc;
//   },{})
      
//   const values = Object.values(obj);
//   const max = Math.max(...values);
//   const countMax = values.filter((v) => v === max).length;
//   return countMax > 1 ? -1 : max;
// }

const array = [1, 2, 3, 3, 3, 4]	// 3

function solution(array) {
  let result;
  

  return result;
}

console.log(solution(arrayBuffer));