const fs = require('fs');
let input = fs.readFileSync('/Users/hayo/Desktop/dev/hojn_dev/co-te/백준/input.txt').toString().split(' '); 

const n = input[0];

function solution(n) {
  let result = n.length;

  return result;
}
solution(n);